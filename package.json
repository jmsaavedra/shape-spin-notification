{
  "name": "spin-shape",
  "version": "1.0.0",
  "description": "This project contains a script that calls the `spin` method on the deployed `MedalSpin` contract every 24.1 hours.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "@josdotph",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.2",
    "ethers": "^6.15.0",
    "express": "^5.1.0"
  }
}
